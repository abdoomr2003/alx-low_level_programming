#include <unistd.h>
void print_alphabet(void);
int _putchar(char c);

int main(void)
{
    void print_alphabet(void)
    {
        int letter;

        for (letter = 'a'; letter <= 'z'; letter++)
        {
                int _putchar(char letter);
                {
                    return (write(1, &c, 1));
                }
        }
                int _putchar(char 10);
                {
                    return (write(1, &c, 1));
                }
    }
    return (0);
}
void print_alphabet(void)
{
        int letter;

        for (letter = 'a'; letter <= 'z'; letter++)
        {
                _putchar (letter);
        }
        _putchar (10);
}

int _putchar(char c)
{
        return (write(1, &c, 1));
}
